# codex_Blog

使用 FastAPI 搭建的简易博客，支持 Markdown 与 Jupyter Notebook 渲染。

## 开发

安装依赖：

```
pip install -r requirements.txt
```

启动开发服务器：

```
make dev
```

后台占位包括登录页 `/admin/login` 与文档上传页 `/admin/upload`。
